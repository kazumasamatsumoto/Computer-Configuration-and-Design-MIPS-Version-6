# コンピュータ内での命令の表現 - 機械語と命令形式

## 1. 命令の数値表現

### 基本概念
本節では、人がコンピュータに命令する形式とコンピュータが命令を受け取る形式との違いを説明する。

コンピュータ内では、命令も高と低の電気信号列で保持されるので、数値として表現することが可能である。実際、命令を構成する1つ1つの信号は1個の数値とみなすことができる。よって、それらの数値を並べたものも命令とみなせる。

### レジスタの命名規則
レジスタはほとんどすべての命令で使用されるので、レジスタ名と番号を対応させる規約が欲しくなる。MIPSのアセンブリ言語では：

- レジスタ `$s0` から `$s7` はレジスタ 16 から 23 に対応
- レジスタ `$t0` から `$t7` はレジスタ 8 から 15 に対応

より具体的には：
- `$s0` はレジスタ 16 を意味
- `$s1` はレジスタ 17 を意味
- `$s2` はレジスタ 18 を意味
- `$t0` はレジスタ 8 を意味
- `$t1` はレジスタ 9 を意味

## 2. MIPSアセンブリ言語命令のマシン命令への翻訳

### 例題：add命令の機械語変換
MIPSの言語をさらに精緻化する次のステップを、例題として取り上げよう。下記の記号表現の命令があるとする。

```assembly
add $t0, $s1, $s2
```

これを実際のMIPSの言語で表したものを、最初は10進数の組み合わせで、ついで2進数の組み合わせで示す。

### 解答：10進数表現
10進数による表現は下記のようになる。

| 0 | 17 | 18 | 8 | 0 | 32 |

### フィールド（Field）の概念
命令のそれぞれの区切られた部分を**フィールド（field）**と呼ぶ。各フィールドの意味：

- **最初のフィールド（0）**: 内容が0のフィールドと32のフィールドの2つで、これが加算命令であることを表す
- **2番目のフィールド（17=$s1）**: 加算の第1オペランドのレジスタの番号
- **3番目のフィールド（18=$s2）**: 加算の第2オペランドのレジスタの番号
- **4番目のフィールド（8=$t0）**: 和を受け取るレジスタの番号
- **5番目のフィールド（0）**: この命令では使用しないので、値が0に設定されている

### 命令形式（Instruction Format）
命令を表記するこのような枠取りを**命令形式（instruction format）**と呼ぶ。

```
| 000000 | 10001 | 10010 | 01000 | 00000 | 100000 |
|  6ビット | 5ビット | 5ビット | 5ビット | 5ビット |  6ビット |
```

上記のビット数を数えてみると分かるとおり、MIPS命令の長さはちょうど32ビットである。つまり、1命令は1語から成る。

## 3. 機械語とマシン・コード

### 定義
- **機械語（machine language）**: 命令を数値で表現したもの、アセンブリ言語と区別して使用
- **マシン・コード（machine code）**: それらの命令を連ねたもの

### 16進数表記の利便性
このように命令が機械語で表現されるなら、長くて味気ない2進数を読み書きしなければならないように思えてくる。基数として2より大きく2進数に変換しやすい数値を使用することによって、そのような退屈な作業を避けることができる。

ほとんどのコンピュータのデータ・サイズは4の倍数なので、**16進数（hexadecimal）**が広く使用されている。16は2のべき乗なので、2進数を4ビットごとに区切って1文字の16進数で置き換えれば簡単に変換できる。

### 16進数と2進数の対応表

| 16進 | 2進 | 16進 | 2進 | 16進 | 2進 | 16進 | 2進 |
|------|-----|------|-----|------|-----|------|-----|
| 0₁₆  | 0000₂ | 4₁₆  | 0100₂ | 8₁₆  | 1000₂ | c₁₆  | 1100₂ |
| 1₁₆  | 0001₂ | 5₁₆  | 0101₂ | 9₁₆  | 1001₂ | d₁₆  | 1101₂ |
| 2₁₆  | 0010₂ | 6₁₆  | 0110₂ | a₁₆  | 1010₂ | e₁₆  | 1110₂ |
| 3₁₆  | 0011₂ | 7₁₆  | 0111₂ | b₁₆  | 1011₂ | f₁₆  | 1111₂ |

## 4. 2進数から16進数への変換

### 例題：進数変換
下記の16進数および2進数の数値を互いに逆に変換せよ。

```
eca8 6420₁₆
0001 0011 0101 0111 1001 1011 1101 1111₂
```

### 解答：変換プロセス
図2.4の対応表を参照して、下記のように変換する。

**16進数から2進数:**
```
eca8 6420₁₆
↓
1110 1100 1010 1000 0110 0100 0010 0000₂
```

**2進数から16進数:**
```
0001 0011 0101 0111 1001 1011 1101 1111₂
↓
1357 9bdf₁₆
```

## 5. MIPSのフィールド構成

### フィールドの詳細
議論がしやすくなるように、MIPS命令の各フィールドには名前が付けられている。

| フィールド | ビット数 | 意味 |
|------------|----------|------|
| **op** | 6ビット | 命令の基本操作。従来から**命令操作コード**（opcode、オペコード）と呼ばれている |
| **rs** | 5ビット | 第1のソース・オペランドのレジスタ |
| **rt** | 5ビット | 第2のソース・オペランドのレジスタ |
| **rd** | 5ビット | デスティネーション・オペランドのレジスタ、つまり結果を収める先 |
| **shamt** | 5ビット | シフト量（シフト命令とこの用語の意味は2.6節で説明する。それまではこのフィールドは使用されないので、値はゼロである） |
| **funct** | 6ビット | 機能。命令操作フィールドのバリエーションを表す。**機能コード**（function code）と呼ばれることがある |

## 6. 設計原則3：優れた設計には適度な妥協が必要である

### 命令形式の統一問題
MIPSの設計者が選択した妥協策は、すべての命令長を同じに保つことであった。その結果、命令の種類によって命令形式が異なる場合が発生する。

### R形式とI形式
上に示した命令形式は**R形式（レジスタ用）**または**Rフォーマット**と呼ぶ。これとは別に**I形式**または**Iフォーマット**という命令形式がある。I形式は即値およびデータ転送命令用であり、下記のフィールド形式をとる。

```
| op | rs | rt | constant または address |
| 6ビット | 5ビット | 5ビット | 16ビット |
```

### lw命令の例
先の例題に示したロード命令をもう一度眺めてみよう。

```assembly
lw $t0, 32($s3)    # A[8]がレジスタ$t0に代入される
```

この場合：
- `$s3`を表す19は**rs**フィールドに入れられ
- `$t0`を表す8は**rt**フィールドに入れられ
- 32は**address**フィールドに入れられる

## 7. MIPSアセンブリ言語の機械語への翻訳

### 実践例題
今度は、プログラムが書くステートメントからマシンが実行するコードまでを通して見ることにしよう。`$t1`には配列Aのベースが保持されており、`$s2`がhに相当するとする。下記のステートメントがある。

```c
A[300] = h + A[300]
```

### コンパイル結果
このステートメントのコンパイル結果は下記のようになる。

```assembly
lw  $t0, 1200($t1)  # A[300]がレジスタ$t0に代入
add $t0, $s2, $t0   # h+A[300]がレジスタ$t0に代入
sw  $t0, 1200($t1)  # h+A[300]をA[300]に戻して格納
```

では、この3つの命令に対するMIPSの機械語コードはどうなるであろうか。

---

## 理解度チェッククイズ

### Q1: 命令形式の理解
MIPS命令が32ビット（1語）で構成される理由と利点を説明してください。

**正解**:
- **理由**: 設計原則3「優れた設計には適度な妥協が必要」に基づく
- **利点**:
  - ハードウェア設計の簡素化
  - 命令フェッチの統一化
  - メモリアクセスの効率化
  - デコード回路の単純化

### Q2: レジスタ命名規則
`$s1`, `$t0`, `$s2`がそれぞれ何番のレジスタに対応するか答えてください。

**正解**:
- `$s1` = レジスタ 17
- `$t0` = レジスタ 8  
- `$s2` = レジスタ 18

### Q3: フィールドの役割
R形式命令の各フィールド（op, rs, rt, rd, shamt, funct）の役割を説明してください。

**正解**:
- **op (6ビット)**: 命令の基本操作（オペコード）
- **rs (5ビット)**: 第1ソース・オペランドのレジスタ
- **rt (5ビット)**: 第2ソース・オペランドのレジスタ  
- **rd (5ビット)**: デスティネーション・レジスタ（結果格納先）
- **shamt (5ビット)**: シフト量（多くの命令では0）
- **funct (6ビット)**: 機能コード（命令操作の詳細指定）

### Q4: 16進数変換
2進数 `1010 1100 0011 1111₂` を16進数に変換してください。

**正解**:
```
1010 1100 0011 1111₂
  ↓    ↓    ↓    ↓
  a    c    3    f
= ac3f₁₆
```

### Q5: 命令形式の区別
R形式とI形式の違いを、用途とフィールド構成の観点から説明してください。

**正解**:
- **R形式**: 
  - 用途: レジスタ間演算（add, sub等）
  - 構成: op(6) + rs(5) + rt(5) + rd(5) + shamt(5) + funct(6)
- **I形式**:
  - 用途: 即値演算、データ転送命令（lw, sw, addi等）
  - 構成: op(6) + rs(5) + rt(5) + constant/address(16)

### Q6: 機械語変換
`add $t0, $s1, $s2` の機械語表現を10進数フィールドで表してください。

**正解**:
| op | rs | rt | rd | shamt | funct |
|----|----|----|----| ------|-------|
| 0  | 17 | 18 | 8  | 0     | 32    |

### Q7: アドレス計算
配列A[300]にアクセスする際のバイトオフセットを計算してください。

**正解**:
- 計算: 300 × 4 = 1200バイト
- 理由: 各語が4バイトのため、インデックスに4を掛ける

---

## プログラミングへの応用

### TypeScript/Angularでの関連概念
- **ビット演算**: 低レベルデータ操作の理解
- **メモリ効率**: データ構造設計での配列アクセスパターン
- **型システム**: 命令形式の概念を型安全性に応用
- **コンパイル過程**: TypeScriptからJavaScriptへの変換理解

### 実践的応用
- **WebAssembly**: 機械語レベルでの最適化理解
- **パフォーマンス最適化**: CPU命令の効率的利用
- **システムプログラミング**: 低レベル操作の基礎知識
- **デバッグ**: 実行時の動作原理理解

これらの概念は、高レベルプログラミング言語でも、コンピュータの動作原理を理解することで、より効率的なコード設計に活かすことができます。

## 8. 実際の機械語命令の決定

### 実践例：3つの命令の機械語変換
便宜上、まず10進数を使用して機械語の命令を表現しよう。配列Aの開始アドレスを与える必要がある。図2.5から3つの機械語命令を決定できる。

| 命令 | op | rs | rt | rd | address/shamt | funct |
|------|----|----|----|----|---------------|-------|
| lw | 35 | 9 | 8 | - | 1200 | - |
| add | 0 | 18 | 8 | 8 | 0 | 32 |
| sw | 43 | 9 | 8 | - | 1200 | - |

### 各命令の詳細解析

**lw命令の解析:**
- **op (最初のフィールド)**: 35である
- **ベース・レジスタ9 ($t1)**: rs（2番目のフィールド）に入れられる
- **デスティネーション・レジスタ8 ($t0)**: rt（3番目のフィールド）に入れられる
- **A[300] (1200 = 300×4)**: を選択するためのオフセットはaddress（最後のフィールド）に入れられる

**add命令の解析:**
- **op (最初のフィールド)**: 0で **funct (最後のフィールド)**: 32である
- **使用される3つのレジスタ (18, 8, 8)**: それぞれ2番目、3番目、4番目のフィールドに入れられる
- これらはレジスタ$s2, $t0, $t0に相当する

**sw命令の解析:**
- **op (最初のフィールド)**: 43である
- この命令の以降のフィールドはlw命令と同じである

### 2進数表現への変換
1200₁₀ = 0000 0100 1011 0000₂ なので、10進表現を2進表現に直すと、下記のようになる。

```
| 100011 | 01001 | 01000 | 0000 0100 1011 0000 |
| 000000 | 10010 | 01000 | 01000 | 00000 | 100000 |
| 101011 | 01001 | 01000 | 0000 0100 1011 0000 |
```

最初の命令と最後の命令がほとんど同じであることに注目してほしい。違うのは左から3番目のビットだけである。

## 9. ハードウェアとソフトウェアのインタフェース

### 命令セット設計の課題
すべての命令のサイズを同じに保ちたいという願いは、レジスタの数を可能な限り多くしたいという願いと、相反する。レジスタの数を増やせば、命令フォーマット中のいずれのレジスタ・フィールドも、少なくとも1ビット多く必要となる。

この制約と、小さければ小さいほど高速であるという設計原則に照らして、今日のほとんどの命令セットは16または32個の汎用レジスタを備えている。

## 10. 根本事項

### コンピュータの2つの基本原理
今日のコンピュータは2つの基本原理に基づいて設計されている：

1. **命令は数値として表現される**
2. **プログラムをメモリ中に格納して、数値と同様に読み書きすることができる**

### プログラム内蔵（Stored-Program）方式
この根本原理から**プログラム内蔵（stored-program）**方式の概念が生まれた。この方式の発明により、コンピュータの世界に魔法のランプの精ジーニーが呼び出された。図2.7にこの概念を示す。メモリ中に種々の要素が格納されていることが分かる。具体的には、エディタ・プログラムのソース・コード、それをコンパイルした結果のマシン・コード、コンパイルされたプログラムが使用するテキスト、さらにはコンパイルを行ったコンパイラまでもが格納されている。

### バイナリ互換性の重要性
命令が数値で表されることの1つの結果と言えるが、プログラムが2進数形式のファイルとして出荷されることがよくある。ここでの商業的な意味は、既存の命令セットとの互換性があれば、コンピュータは既存のソフトウェアを継承できることである。このような「バイナリ互換性」を求めることは、少数の命令セット・アーキテクチャを制にして業界が運営することにつながる。

## 11. 自己診断問題

### MIPS命令の解析
次のMIPS命令は何を表すか。下の4つの選択肢の中から1つを選べ。

| op | rs | rt | rd | shamt | funct |
|----|----|----|----| ------|-------|
| 0  | 8  | 9  | 10 | 0     | 34    |

#### 選択肢：
1. sub $t0, $t1, $t2
2. add $t2, $t0, $t1  
3. sub $t2, $t1, $t0
4. sub $t2, $t0, $t1

#### 解答プロセス
レジスタ番号の対応：
- レジスタ8 = $t0
- レジスタ9 = $t1  
- レジスタ10 = $t2

R形式命令では：
- rs = 第1ソース（レジスタ8 = $t0）
- rt = 第2ソース（レジスタ9 = $t1）
- rd = デスティネーション（レジスタ10 = $t2）
- funct = 34 はsub命令を示す

したがって：**sub $t2, $t0, $t1** が正解（選択肢4）

### 16進数変換問題
ある人の年齢が10進数で40であるならば、16進数ではいくつか。

#### 解答：
```
40 ÷ 16 = 2 余り 8
2 ÷ 16 = 0 余り 2
```
余りを逆順に読むと：**28₁₆**

## 12. 用語解説

### 重要用語の定義

**命令形式（instruction format）**: 2進数のフィールドで構成された、命令表記のための枠取り

**機械語（machine language）**: コンピュータ・システム内で命令やデータをやり取りするときに使う2進表現

**16進数（hexadecimal）**: 基数を16とする数値

**命令操作コード（opcode）**: 命令の操作種別と形式を表すフィールド

---

## 追加理解度チェッククイズ

### Q8: 機械語変換の実践
`lw $t0, 1200($t1)` の機械語表現（10進数フィールド）を作成してください。

**正解**:
| op | rs | rt | address |
|----|----|----|---------|
| 35 | 9  | 8  | 1200    |

### Q9: プログラム内蔵方式
プログラム内蔵方式の2つの基本原理とその意義を説明してください。

**正解**:
1. **命令は数値として表現される**: 命令とデータの統一的な扱いが可能
2. **プログラムをメモリ中に格納**: 実行時の動的な変更や自己修正が可能

**意義**: コンピュータの汎用性と柔軟性を実現し、ソフトウェアによる制御を可能にした

### Q10: バイナリ互換性
バイナリ互換性が業界に与える影響を説明してください。

**正解**:
- **ソフトウェア資産の継承**: 既存プログラムの再利用可能
- **開発コスト削減**: 移植作業が不要
- **市場の標準化**: 少数の命令セット・アーキテクチャへの集約
- **技術革新の制約**: 既存との互換性が新技術導入の障壁となる場合もある