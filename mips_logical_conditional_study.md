# MIPSアーキテクチャ：論理演算と条件判定用命令 学習資料

## 2.6 論理演算

### 主要な論理演算命令

| 論理演算 | C演算子 | Java演算子 | MIPS命令 |
|---------|---------|------------|----------|
| 左シフト | << | << | sll |
| 右シフト | >> | >>> | srl |
| ビット単位のAND | & | & | and, andi |
| ビット単位のOR | \| | \| | or, ori |
| ビット単位のNOT | ~ | ~ | nor |

### シフト演算の原理

**左シフト (sll - shift left logical)**
- 全ビットを左に移動し、右側に0を詰める
- 例: `sll $t2, $s0, 4` → $s0を4ビット左シフトして$t2に格納
- 数学的意味: 2^n倍（nは左シフト数）

**右シフト (srl - shift right logical)**
- 全ビットを右に移動し、左側に0を詰める
- 論理右シフトは符号なし整数用

### 論理演算の応用

**AND演算とマスク**
- マスクを使って特定のビットを「隠す」
- 例: `and $t0, $t1, $t2` で、$t1と$t2の両方が1のビットのみ1になる

**OR演算**
- 2つのオペランドのどちらか一方が1の場合に結果を1にする
- ビットの設定に使用

**NOT演算 (NOR)**
- MIPSではNOTの代わりにNOR命令を使用
- A NOR 0 = NOT(A OR 0) = NOT(A)

## 2.7 条件判定用命令

### 条件分岐命令

**beq (branch if equal)**
```
beq register1, register2, L1
```
- register1とregister2が等しい場合、ラベルL1にジャンプ

**bne (branch if not equal)**
```
bne register1, register2, L1
```
- register1とregister2が等しくない場合、ラベルL1にジャンプ

### 比較命令

**slt (set on less than)**
```
slt $t0, $s3, $s4
```
- $s3 < $s4の場合、$t0に1を設定、そうでなければ0を設定

**slti (set on less than immediate)**
```
slti $t0, $s2, 10
```
- $s2 < 10の場合、$t0に1を設定

### if-then-else構造のコンパイル

**高級言語のif文**
```c
if (i == j) f = g + h; else f = g - h;
```

**対応するMIPSコード**
```assembly
bne $s3, $s4, Else    # i ≠ j なら Else へ
add $s0, $s1, $s2     # f = g + h (i == j の場合)
j Exit                # Exit へジャンプ
Else: sub $s0, $s1, $s2   # f = g - h
Exit:
```

### ループ構造

**while文**
```c
while (save[i] == k)
    i += 1;
```

**対応するMIPSコード**
```assembly
Loop: sll $t1, $s3, 2     # i * 4 (配列のインデックス計算)
      add $t1, $t1, $s6   # save配列のベースアドレス + オフセット
      lw $t0, 0($t1)      # save[i]をロード
      bne $t0, $s5, Exit  # save[i] ≠ k なら Exit へ
      addi $s3, $s3, 1    # i = i + 1
      j Loop              # ループの先頭に戻る
Exit:
```

## 重要な原理原則

### 1. ハードウェア設計原則
- **基本ブロック**: 分岐のない命令列
- コンパイラは基本ブロック単位で最適化を行う

### 2. 符号付き数と符号なし数の比較
- **符号付き**: slt, slti
- **符号なし**: sltu, sltiu
- 符号の違いにより比較結果が変わる可能性がある

### 3. ジャンプアドレステーブル
- case/switch文の効率的な実装
- jr (jump register)命令を使用
- 条件の数が多い場合に有効

## 学習のポイント

1. **ビット操作の理解**: シフト演算とマスクの概念をしっかり理解する
2. **制御フローの変換**: 高級言語の制御構造をアセンブリ言語に変換する方法
3. **効率的なコード生成**: コンパイラがどのように最適化を行うか
4. **ハードウェアとソフトウェアの協調**: 命令セットアーキテクチャの設計思想

---

## 理解度確認クイズ

### 問題1: シフト演算
次のMIPS命令の実行後、レジスタ$t0の値は何になるか？
初期値: $s0 = 0x12345678
```assembly
sll $t0, $s0, 4
```

### 問題2: 論理演算
以下の2つのレジスタに対してAND演算を行った結果は？
- $t1 = 0x0F0F0F0F
- $t2 = 0xFF00FF00
```assembly
and $t0, $t1, $t2
```

### 問題3: 条件分岐
以下のC言語コードをMIPSアセンブリに変換せよ：
```c
if (a > b) {
    c = a - b;
} else {
    c = b - a;
}
```
（変数a, b, cはそれぞれ$s0, $s1, $s2に格納されているとする）

### 問題4: ループ構造
以下のfor文をMIPSアセンブリに変換せよ：
```c
for (i = 0; i < 10; i++) {
    sum += array[i];
}
```

---

**学習のヒント**: 
- 実際にビット演算を手で計算してみる
- フローチャートを描いて制御の流れを視覚化する
- MIPSシミュレータを使って動作を確認する