# MIPS手続き（プロシージャ）学習ガイド

## 1. 手続きの基本概念

### 手続きとは
- プログラムを構成する**再利用可能な**コードの単位
- **抽象化**を実現する重要な手法
- パラメータによる**インターフェース**を提供

### 手続き実行の6つのステップ
1. **パラメータをアクセス可能な場所に配置**
2. **制御を手続きに移す**
3. **必要なメモリ資源を入手**
4. **必要なタスクを実行**
5. **結果を呼び出し元がアクセス可能な場所に配置**
6. **制御を呼び出し元に戻す**

## 2. MIPSレジスタ使用規約

### 引数レジスタ
- **$a0-$a3**: パラメータ渡し用（4本）
- 4つを超える引数は**スタック**を使用

### 戻り値レジスタ
- **$v0-$v1**: 結果の値を返すため（2本）

### 戻りアドレスレジスタ
- **$ra**: 手続きから戻るアドレスを保存
- `jal`命令で自動的に設定

### 一時レジスタの分類
- **$t0-$t9**: 手続き呼び出し時に**保存されない**
- **$s0-$s7**: 手続き呼び出し時に**保存される**
- 呼び出し側は使用前後で値の復元が必要

## 3. ジャンプ・リンク命令

### jal命令（Jump and Link）
```assembly
jal procedure_name
```
- **PC+4**を`$ra`に保存
- 指定したアドレスにジャンプ

### jr命令（Jump Register）
```assembly
jr $ra
```
- レジスタに格納されたアドレスに無条件ジャンプ
- 手続きからの戻りに使用

## 4. スタックの仕組み

### スタックの特徴
- **LIFO**（Last In, First Out）構造
- **高いアドレス→低いアドレス**に向かって成長
- **スタックポインタ（$sp）**で管理

### スタック操作
```assembly
# スタックにデータをプッシュ
addi $sp, $sp, -4    # スペース確保
sw   $t0, 0($sp)     # データ保存

# スタックからデータをポップ
lw   $t0, 0($sp)     # データ復元
addi $sp, $sp, 4     # スペース解放
```

## 5. リーフ手続きと非リーフ手続き

### リーフ手続き
- **他の手続きを呼び出さない**手続き
- 比較的単純な実装
- `$ra`の保存が不要な場合が多い

### 非リーフ手続き
- **他の手続きを呼び出す**手続き
- `$ra`と使用レジスタの**スタック保存**が必要
- より複雑なスタック管理が必要

## 6. 再帰手続きの実装

### 階乗計算の例
```c
int fact(int n) {
    if (n < 1) return 1;
    else return (n * fact(n-1));
}
```

### MIPSでの実装ポイント
- **ベースケース**の適切な処理
- **スタックフレーム**での状態管理
- 引数と戻り値の正確な受け渡し

## 7. メモリレイアウト

### 主要セグメント
- **テキストセグメント**: 機械語コード
- **静的データセグメント**: グローバル変数
- **ヒープ**: 動的メモリ割り当て
- **スタック**: 手続き呼び出し管理

### フレームポインタ（$fp）
- **手続きフレーム**の基準点
- ローカル変数への安定したアクセス
- デバッグ時に有用

## 学習のポイント

1. **レジスタ使用規約**の完全な理解
2. **スタック操作**の正確な実装
3. **手続き呼び出し前後**での適切な保存・復元
4. **再帰呼び出し**におけるスタック管理
5. **メモリセグメント**の役割と相互作用

---

## 練習問題

### 基礎問題
1. MIPSで引数を渡すために使用するレジスタを4つ答えよ
2. `jal`命令が実行される時、戻りアドレスはどこに保存されるか
3. スタックが成長する方向は？

### 応用問題
1. 5つの引数を持つ手続きを呼び出す場合、5番目の引数はどこに配置されるか
2. 非リーフ手続きで必ず保存すべきレジスタは何か
3. 再帰呼び出しで無限ループを防ぐために必要な条件は？

### 実装問題
フィボナッチ数列を計算する再帰手続きをMIPSアセンブリで実装する際の
主要な考慮点を3つ挙げよ

---

## 練習問題 解答と解説

### 基礎問題 解答

**問1: MIPSで引数を渡すために使用するレジスタを4つ答えよ**
```
解答: $a0, $a1, $a2, $a3
```
**解説:**
- MIPSでは最初の4つの引数専用レジスタが予約されている
- これらは**argument registers**と呼ばれる
- 5つ目以降の引数はスタックを使用する必要がある
- この規約により、多くの手続き呼び出しが効率的に実行できる

**問2: `jal`命令が実行される時、戻りアドレスはどこに保存されるか**
```
解答: $ra（戻りアドレスレジスタ）
```
**解説:**
- `jal`（Jump and Link）命令は2つの動作を同時に行う
  1. **PC+4**（次の命令のアドレス）を`$ra`に保存
  2. 指定されたアドレスにジャンプ
- `PC+4`が保存される理由：MIPSでは命令が4バイト固定長のため
- 手続きから戻る際は`jr $ra`を使用

**問3: スタックが成長する方向は？**
```
解答: 高いアドレスから低いアドレスへ（下向き）
```
**解説:**
- スタックポインタ（$sp）は使用されるメモリの**最低アドレス**を指す
- データをプッシュする際：`addi $sp, $sp, -4`（アドレスを減算）
- データをポップする際：`addi $sp, $sp, 4`（アドレスを加算）
- この方式により、ヒープとスタックが効率的にメモリ空間を共有できる

### 応用問題 解答

**問1: 5つの引数を持つ手続きを呼び出す場合、5番目の引数はどこに配置されるか**
```
解答: スタック上
```
**解説:**
```assembly
# 5つの引数（arg1, arg2, arg3, arg4, arg5）を渡す例
# arg1-arg4はレジスタへ
move $a0, $t0    # arg1 → $a0
move $a1, $t1    # arg2 → $a1  
move $a2, $t2    # arg3 → $a2
move $a3, $t3    # arg4 → $a3

# arg5はスタックへ
addi $sp, $sp, -4
sw   $t4, 0($sp) # arg5をスタックにプッシュ

jal  procedure   # 手続き呼び出し

addi $sp, $sp, 4 # スタックを元に戻す
```
- レジスタ引数は高速だが、数に制限がある
- スタック引数は低速だが、理論上無制限

**問2: 非リーフ手続きで必ず保存すべきレジスタは何か**
```
解答: $ra（戻りアドレスレジスタ）
```
**解説:**
```assembly
non_leaf_procedure:
    # $raを保存（他の手続きを呼ぶため）
    addi $sp, $sp, -4
    sw   $ra, 0($sp)
    
    # 他の手続きを呼び出し
    jal  other_procedure  # $raが上書きされる
    
    # $raを復元
    lw   $ra, 0($sp)
    addi $sp, $sp, 4
    
    jr   $ra             # 元の呼び出し元に戻る
```
- 非リーフ手続きは他の手続きを呼び出すため、`jal`により`$ra`が上書きされる
- 保存しないと、**元の呼び出し元に戻れなくなる**
- その他の保存対象：使用する`$s`レジスタ、必要に応じて`$a`レジスタ

**問3: 再帰呼び出しで無限ループを防ぐために必要な条件は？**
```
解答: ベースケース（終了条件）の適切な実装
```
**解説:**
再帰には以下の要素が必須：
1. **ベースケース**: 再帰を止める条件
2. **再帰ケース**: 問題を小さくして自分自身を呼び出す
3. **収束性**: 必ずベースケースに到達すること

```assembly
# 階乗計算の例
factorial:
    # ベースケース: n < 1 なら 1 を返す
    slti $t0, $a0, 1      # $t0 = (n < 1) ? 1 : 0
    beq  $t0, $zero, recursive_case
    
    # n < 1 の場合
    addi $v0, $zero, 1    # return 1
    jr   $ra
    
recursive_case:
    # n >= 1 の場合: n * fact(n-1)
    # ... スタック保存・再帰呼び出し・結果計算
```

### 実装問題 解答

**フィボナッチ数列の再帰実装における主要考慮点**

**1. ベースケースの適切な処理**
```assembly
fibonacci:
    # ベースケース1: fib(0) = 0
    beq  $a0, $zero, base_case_0
    # ベースケース2: fib(1) = 1  
    addi $t0, $zero, 1
    beq  $a0, $t0, base_case_1
```

**2. スタックフレーム管理**
```assembly
    # レジスタ保存（$ra, $a0, $s0が必要）
    addi $sp, $sp, -12    # 3つのワード分確保
    sw   $ra, 8($sp)      # 戻りアドレス
    sw   $a0, 4($sp)      # 元のn値
    sw   $s0, 0($sp)      # fib(n-1)の結果保存用
    
    # fib(n-1)計算
    addi $a0, $a0, -1
    jal  fibonacci
    move $s0, $v0         # fib(n-1)を保存
    
    # fib(n-2)計算  
    lw   $a0, 4($sp)      # 元のn値復元
    addi $a0, $a0, -2
    jal  fibonacci
    
    # 結果計算: fib(n-1) + fib(n-2)
    add  $v0, $s0, $v0
    
    # レジスタ復元
    lw   $s0, 0($sp)
    lw   $a0, 4($sp)  
    lw   $ra, 8($sp)
    addi $sp, $sp, 12
```

**3. 効率性の考慮**
- フィボナッチの素朴な再帰実装は**指数的時間計算量**O(2^n)
- 同じ値を何度も計算する**重複計算問題**
- 実用的には**動的プログラミング**や**反復実装**を検討

**TypeScript/Angularとの比較ポイント:**
```typescript
// TypeScript版（参考）
function fibonacci(n: number): number {
    if (n <= 1) return n;  // ベースケース
    return fibonacci(n-1) + fibonacci(n-2);  // 再帰ケース
}
```
- 高水準言語では**自動的なスタック管理**
- MIPSでは**明示的なレジスタ・スタック操作**が必要
- どちらも**同じアルゴリズム的思考**が重要