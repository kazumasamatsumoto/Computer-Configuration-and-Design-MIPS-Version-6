# 符号付き数と符号なし数 - 2進数表現とオーバーフロー

## 1. 数値表現の基礎

### 10進数と2進数
まず、コンピュータ内で数値がどのように表現されるかを簡単に見ておこう。指が10本あるため、人は数を**10進数（decimal number）**として習っている。しかし、数値は何進法でも表現できる。たとえば、10進数の123は2進法では1111011と表現される。

### バイナリ・ディジット（ビット）
コンピュータ内では、数値は高と低の電気信号列の形で保持される。したがって、コンピュータ内の数値は**2進数（binary number）**である。2進数の1文字はコンピュータ処理の「原子」の役割を担う。すなわち、すべての情報は**バイナリ・ディジット（binary digit）**すなわち**ビット（bit）**を単位にして構成されるからである。

### 基数変換の一般式
一般化すると、基数が何であれ、i番目の位の数dの値は下記の式で表される：

```
d × 基数^i
```

ここで、iは0から始め、右から左に向かって繰り上げる。

### MIPSの32ビット語
MIPSの1語長は32ビットである。32ビットのパターンは2³²通りある。したがって、それらのビット・パターンによって、0から2³² - 1（4,294,967,295₁₀）までの数値を表すことができる。

## 2. ビット位置の命名規則

### 最下位ビットと最上位ビット
語は横だけではなく縦に書かれることもあるので、左端とか右端といった表現はあいまいである。そこで、右端のビット（上図のビット0）を表すときは**最下位ビット（least significant bit）**、左端のビット（上図のビット31）を表すときは**最上位ビット（most significant bit）**という言葉を使用することにする。

### 32ビットパターンの例
```
0000 0000 0000 0000 0000 0000 0000 0000₂ = 0₁₀
0000 0000 0000 0000 0000 0000 0000 0001₂ = 1₁₀
0000 0000 0000 0000 0000 0000 0000 0010₂ = 2₁₀
...
1111 1111 1111 1111 1111 1111 1111 1101₂ = 4,294,967,293₁₀
1111 1111 1111 1111 1111 1111 1111 1110₂ = 4,294,967,294₁₀
1111 1111 1111 1111 1111 1111 1111 1111₂ = 4,294,967,295₁₀
```

## 3. 符号なし数と符号付き数の問題

### 歴史的背景
人間にとって基数2は自然ではない。人には指が10本あるので、基数10が自然である。では、コンピュータではなぜ10進数を使用しないのだろうか。実際には、初期の商用コンピュータでは10進数の演算が提供されていた。問題は、コンピュータの内部ではいずれにせよオンおよびオフの信号が使用されていたことである。

### 正と負の数の表現問題
コンピュータ・プログラムは正の数と負の数を扱うので、両者を区別する表現方法が必要となる。一番分かりやすい解決策は、1ビットで表現できる符号を別に追加することである。この方法を**符号付き絶対値（sign and magnitude）**表現と呼ぶ。

### 符号付き絶対値表現の欠陥
残念ながら、符号付き絶対値表現には欠陥がいくつかある：

1. **符号ビットの位置**: 符号ビットをどこに置くかが定まらない（右にすべきか、それとも左にすべきか）
2. **計算の複雑さ**: 計算結果の符号を設定するために、加算器に余分なステップが必要となる
3. **ゼロの重複**: ゼロに正と負の2つがあることになる（不注意なプログラムの問題原因）

## 4. 2の補数表現

### 2の補数とは
最終的な解決策はハードウェアを単純にする方向で落ち着いた。すなわち、上位に0が先行する場合は正を意味するものとし、上位に1が先行する場合は負を意味するものとしたのである。符号付き2進数に関するこの表現方法を**2の補数（two's complement）**表現と呼ぶ。

### 2の補数の利点
2の補数表現の利点には、負の数の最上位ビットの値がすべて1となることが挙げられる。その結果、ハードウェアで数値の正負判定（0は正とみなす）をする場合、最上位ビットを検査するだけで済む。

### 符号ビットを利用した表現
符号ビットを利用すると、正の数も負の数も下記の式で表すことができる：

```
(x31 × −2³¹) + (x30 × 2³⁰) + (x29 × 2²⁹) + ... + (x1 × 2¹) + (x0 × 2⁰)
```

ここで、xiは数値xのi番目のビットを表す。符号ビットには−2³¹を掛け、残りの各ビットには正の2を位数でべき乗したものを掛ける。

## 5. 2進から10進への変換

### 例題：2の補数の10進値変換
下記の32ビットの2の補数の10進値はいくつか。

```
1111 1111 1111 1111 1111 1111 1111 1100₂
```

### 解答：ビット値代入計算
上記の公式にこの数値の各ビットの値を代入すると、下記の結果が得られる：

```
(1 × −2³¹) + (1 × 2³⁰) + (1 × 2²⁹) + ... + (1 × 2²) + (0 × 2¹) + (0 × 2⁰)
= −2³¹ + 2³⁰ + 2²⁹ + ... + 2²
= −2,147,483,648₁₀ + 2,147,483,644₁₀
= −4₁₀
```

## 6. オーバーフローの概念

### オーバーフローとは
符号なしの数値を操作した結果の数値がハードウェアの容量をオーバーフローする可能性があるのと同様に、2の補数に対する操作でもオーバーフローが発生しうる。

### オーバーフロー発生条件
2進ビット・パターンの左端のビットが左側に無限に続くべき数字と等しくない（符号ビットが正しくない）場合に、オーバーフローが発生している。具体的には：

- **負の数値の場合**: 左側のビット・パターンが0である場合
- **正の数値の場合**: 左側のビット・パターンが1である場合

---

## 理解度チェッククイズ

### Q1: 基数変換の理解
10進数の123を2進数に変換し、その変換過程を説明してください。

**正解**:
```
123 ÷ 2 = 61 余り 1
61 ÷ 2 = 30 余り 1
30 ÷ 2 = 15 余り 0
15 ÷ 2 = 7 余り 1
7 ÷ 2 = 3 余り 1
3 ÷ 2 = 1 余り 1
1 ÷ 2 = 0 余り 1
```
余りを逆順に読むと：**1111011₂**

### Q2: ビット位置の命名
32ビット語において、ビット0とビット31はそれぞれ何と呼ばれるか答えてください。

**正解**:
- **ビット0（右端）**: 最下位ビット（least significant bit）
- **ビット31（左端）**: 最上位ビット（most significant bit）

### Q3: 符号付き絶対値表現の問題点
符号付き絶対値表現が採用されなかった理由を3つ挙げてください。

**正解**:
1. **符号ビットの位置**: どこに符号ビットを置くかが定まらない
2. **計算の複雑さ**: 加算器に余分なステップが必要
3. **ゼロの重複**: 正のゼロと負のゼロが存在してしまう

### Q4: 2の補数変換
2の補数表現で `1111 1111 1111 1111 1111 1111 1111 1110₂` を10進数に変換してください。

**正解**:
```
(1 × −2³¹) + (1 × 2³⁰) + ... + (1 × 2¹) + (0 × 2⁰)
= −2³¹ + (2³¹ − 2)
= −2³¹ + 2³¹ − 2
= −2₁₀
```

### Q5: オーバーフロー検出
2の補数演算でオーバーフローが発生する条件を説明してください。

**正解**:
- **正の数の場合**: 結果の最上位ビットが1になった場合（負に見える）
- **負の数の場合**: 結果の最上位ビットが0になった場合（正に見える）
- **一般的な判定**: 符号ビットが期待される値と異なる場合

### Q6: 2の補数の利点
2の補数表現がハードウェア設計において優れている理由を説明してください。

**正解**:
- **正負判定の簡素化**: 最上位ビットを確認するだけで正負が判定できる
- **加算回路の統一**: 正の数も負の数も同じ加算回路で処理可能
- **ゼロの一意性**: ゼロの表現が一つだけ
- **ハードウェアの単純化**: 特別な符号処理回路が不要

---

## プログラミングへの応用

### TypeScript/Angularでの数値処理
- **ビット演算**: 2の補数理解によるビット操作の最適化
- **オーバーフロー対策**: 大きな数値計算時の境界値チェック
- **型安全性**: 符号付き/符号なし数値の適切な使い分け
- **パフォーマンス**: ビットレベルでの効率的な数値操作

### 実践的な応用例
- **フラグ管理**: ビット演算を使用した状態管理
- **暗号化**: ビットレベルでのデータ変換
- **画像処理**: ピクセルデータの効率的な操作
- **ネットワーク**: バイナリプロトコルの実装

これらの概念は、低レベルプログラミングだけでなく、効率的な高レベルプログラムの作成にも重要な基礎知識となります。