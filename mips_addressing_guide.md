# MIPS アドレッシング方式 完全ガイド

## 概要

MIPSアーキテクチャにおけるアドレッシング方式は、命令でメモリアドレスやオペランドを指定する方法です。各方式には特定の用途があり、効率的なプログラムを作成するために理解が重要です。

## 5つのアドレッシング・モード

### 1. 即値アドレッシング (Immediate Addressing)

**原理**: 命令中に定数値を直接指定する方式

**特徴**:
- オペランドが命令の一部として格納される
- メモリアクセスが不要で高速
- 16ビットの即値を使用（-32,768 ～ 32,767）

**例**:
```assembly
addi $t0, $t1, 100  # $t0 = $t1 + 100
ori  $s0, $s0, 2304 # $s0 = $s0 | 2304
```

**命令フォーマット**: I形式
```
[op(6)] [rs(5)] [rt(5)] [immediate(16)]
```

### 2. レジスタ・アドレッシング (Register Addressing)

**原理**: オペランドがレジスタに格納されている方式

**特徴**:
- 最も高速なアクセス
- レジスタファイル内の32個のレジスタを使用
- 算術演算、論理演算で主に使用

**例**:
```assembly
add $t0, $t1, $t2   # $t0 = $t1 + $t2
and $s0, $s1, $s2   # $s0 = $s1 & $s2
```

**命令フォーマット**: R形式
```
[op(6)] [rs(5)] [rt(5)] [rd(5)] [shamt(5)] [funct(6)]
```

### 3. ベース相対アドレッシング (Base Addressing)

**原理**: ベースレジスタの値に定数を加算してアドレスを計算

**特徴**:
- メモリアクセス命令（load/store）で使用
- 16ビットのオフセット（-32,768 ～ 32,767）
- 配列やデータ構造のアクセスに適している

**例**:
```assembly
lw  $t0, 4($s0)     # $t0 = Memory[$s0 + 4]
sw  $t1, 8($sp)     # Memory[$sp + 8] = $t1
```

**アドレス計算**: `実効アドレス = レジスタ値 + オフセット`

### 4. PC相対アドレッシング (PC-relative Addressing)

**原理**: プログラムカウンタ（PC）を基準とした相対アドレス指定

**特徴**:
- 条件分岐命令で使用
- 16ビットのオフセットを4倍して使用
- 分岐範囲: ±128KB（±32K命令）

**例**:
```assembly
beq  $t0, $t1, Loop  # if ($t0 == $t1) goto Loop
bne  $s0, $s1, Exit  # if ($s0 != $s1) goto Exit
```

**アドレス計算**: `分岐先 = PC + 4 + (オフセット × 4)`

### 5. 擬似直接アドレッシング (Pseudodirect Addressing)

**原理**: 命令内の26ビットアドレスとPCの上位ビットを結合

**特徴**:
- ジャンプ命令（j, jal）で使用
- 26ビットのアドレスフィールド
- 256MB の範囲内でジャンプ可能

**例**:
```assembly
j    Main           # Main ラベルにジャンプ
jal  Function       # Function を呼び出し（戻りアドレスを$raに保存）
```

**アドレス計算**: `ジャンプ先 = {PC[31:28], アドレス[25:0], 00}`

## 命令フォーマットとの対応

| アドレッシング方式 | 命令フォーマット | 主な用途 |
|-------------------|-----------------|----------|
| 即値              | I形式           | 定数演算 |
| レジスタ          | R形式           | レジスタ間演算 |
| ベース相対        | I形式           | メモリアクセス |
| PC相対           | I形式           | 条件分岐 |
| 擬似直接          | J形式           | 無条件ジャンプ |

## 実例: MIPSアセンブリコードの解析

```assembly
# データセクション
.data
array: .word 10, 20, 30, 40, 50

# テキストセクション
.text
main:
    # 即値アドレッシング
    addi $t0, $zero, 5      # $t0 = 5
    
    # ベース相対アドレッシング
    la   $s0, array         # $s0 = array のアドレス
    lw   $t1, 0($s0)        # $t1 = array[0] = 10
    lw   $t2, 4($s0)        # $t2 = array[1] = 20
    
    # レジスタアドレッシング
    add  $t3, $t1, $t2      # $t3 = $t1 + $t2 = 30
    
    # PC相対アドレッシング
    beq  $t3, $t0, equal    # if ($t3 == $t0) goto equal
    
    # 擬似直接アドレッシング
    j    end                # end にジャンプ

equal:
    # 処理...
    
end:
    # プログラム終了
```

## 理解度チェッククイズ

### 【基礎レベル】

**問題1**: 次の命令で使用されているアドレッシング方式を答えてください。
```assembly
addi $t0, $t1, 100
```

<details>
<summary>解答</summary>

**即値アドレッシング**

解説: 定数値100が命令に直接埋め込まれているため、即値アドレッシングです。
</details>

**問題2**: ベース相対アドレッシングで使用できるオフセットの範囲は？

<details>
<summary>解答</summary>

**-32,768 ～ 32,767（16ビット符号付き整数）**

解説: MIPSのI形式命令では16ビットのimmediateフィールドが使用されます。
</details>

### 【応用レベル】

**問題3**: 次のコードで実際にアクセスされるメモリアドレスを計算してください。
```assembly
# $s0 = 0x10000000 とする
lw $t0, 12($s0)
```

<details>
<summary>解答</summary>

**0x1000000C**

解説: ベース相対アドレッシングなので、実効アドレス = $s0 + オフセット = 0x10000000 + 12 = 0x1000000C
</details>

**問題4**: PC相対アドレッシングで、現在のPC=0x00400000、分岐オフセット=8の場合、分岐先アドレスは？

<details>
<summary>解答</summary>

**0x00400024**

解説: 
- 分岐先 = PC + 4 + (オフセット × 4)
- = 0x00400000 + 4 + (8 × 4)
- = 0x00400000 + 4 + 32
- = 0x00400024
</details>

### 【発展レベル】

**問題5**: 次の機械語命令を解析してください。
```
0x20080064  # 2進数: 00100000000010000000000001100100
```

この命令の：
- 命令形式
- オペコード
- 使用しているアドレッシング方式
- アセンブリ表記

を答えてください。

<details>
<summary>解答</summary>

**命令形式**: I形式

**オペコード**: 001000 (8) → addi命令

**アドレッシング方式**: 即値アドレッシング

**フィールド分解**:
- op: 001000 (addi)
- rs: 00000 ($zero)
- rt: 01000 ($t0)
- immediate: 0000000001100100 (100)

**アセンブリ表記**: `addi $t0, $zero, 100`
</details>

## まとめ

MIPSの5つのアドレッシング方式は、それぞれ異なる用途に最適化されています：

1. **即値**: 定数値の高速処理
2. **レジスタ**: 最高速のデータ操作
3. **ベース相対**: 柔軟なメモリアクセス
4. **PC相対**: 効率的な分岐制御
5. **擬似直接**: 広範囲ジャンプ

これらの方式を適切に理解し使い分けることで、効率的なMIPSプログラムを作成できます。各方式の特徴と制限を把握し、プログラムの要件に応じて最適な方式を選択することが重要です。

## 参考資料

- パターソン&ヘネシー「コンピュータの構成と設計」
- MIPS32™ Architecture For Programmers
- 各種MIPSアセンブラ・シミュレータ